---
interface Props {
    date: Date;
}
const { date } = Astro.props;
---

<time
datetime={date.toISOString()}
class="pl-2.5 mt-6 text-sm font-semibold text-zinc-400"
>
{
    date.toLocaleDateString("es-es", {
        year: "numeric",
        month: "short",
        day: "numeric",
    })
}
</time>